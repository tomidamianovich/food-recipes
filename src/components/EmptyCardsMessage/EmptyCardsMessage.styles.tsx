import {
	createStyles,
	Theme,
} from '@material-ui/core/styles';

const styles = (theme: Theme) =>
  createStyles({
    wrapper: {
      display: 'block',
      textAlign: 'center'
    }
  });

export default styles